# .goreleaser.yml
brews:
  - 
    name: awls
    tap:
      owner: danpilch
      name: homebrew-awls
      token: "{{ .Env.GITHUB_TOKEN }}"
    folder: Formula
    description: "Lookup EC2 information in the terminal"
    license: "MIT"
